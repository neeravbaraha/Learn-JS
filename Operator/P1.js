//Create a program to calculate the area of a rectangle (length * width) and the perimeter (2 * (length + width)) based on user input.

let length=3;
let width=4;

let area =length*width;
let perimeter=(2*(length+width));
console.log("Area of rectangle=",area);
console.log("Perimeter of rectangle=",perimeter);